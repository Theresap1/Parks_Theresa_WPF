/**
 * Created by theresaparks on 7/27/14.
 */

//alert("1,2,3");

//function functionName() {
    //code the function runs
//}


// curly braces are for blocks of code
// parenthesis are for parameters
//functions have names so we can refer to them later.


//function outptMsg(){
    //console.log("Good Morning World");
//}



/*var width = 5;

function calcArea(){
    var width = 28;
    var height =30;
    var area = width * height;
    console.log(area);
}


calcArea();
calcArea();
calcArea();

// variable scope
// inside and outside of functions

console.log(width);

//if it is called outside the function it will use the var that is outside
//if it is called on the inside of the function then it will use the var in the function
*/

/*Arguments and Parameters

 */


/*functionName (argument1, argument2);

function funcName (parameter1, parameter2){
    //code the function runs
}
calcArea(30, 20);//arguments
// inside the () need to becareful of the order they are put in

function calcArea(w,h){ // w=30 h=20    // parameters
    var area =w * h;
    console.log(area);
}
*/
function dogYears(age){ //put age in the () to be able to get multiple ansers when called
    //var age =4;
    var dogYears = age * 7;
    console.log("Sparky is "+ dogYears + "years old.")
}
//has to be called to work
dogYears(age1);
dogYears(2);
var age1 = 4;// get same results



















